FRC tabletop simulator for deep space

Uses python 3
Uses pygame

to run install python 3 and pygame (good luck with that)
python main.py


Notes:
-------------------------------------------------------------------------

01/13/18 JSA

      keymaps and joysticks are now in keymaps.txt
      joysticks are assigned as they are added
      Robots can now be tank, mecanum (body frame), or mecanum(field frame)

      cargo ship is maybe too small
      collision detection at 45 degree hangs up robots, especially on cargo


      

